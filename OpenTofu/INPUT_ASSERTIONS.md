Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Input → Output Assertion Findings

These items in the wizard spec are not represented in the rendered
output. Each is a divergence between the user's intent and the deployed
Landing Zone.

- **Workload account without VPC** `AppSandbox` — Declared in 02_account_hierarchy.workload_accounts but no VPC in 04_networking.vpcs deploys into it — the account is created with no network, so nothing can run there. Add a VPC for it (the wizard's environment expansion does this automatically for canonical environment accounts).
- **Unsupported section** `EKS & Containers (12_eks_containers)` — Section is enabled with content (eks_clusters, karpenter_node_pools) but is NOT emitted in any AWS output format (LZA / OpenTofu / CDK / tfvars). Provision these resources out of band or via a custom module — the wizard does not yet generate them.
