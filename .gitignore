# Terraform / OpenTofu
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
crash.log
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CDK / Node (if a CDK format is added later)
node_modules/
cdk.out/
*.js.map
.cdk.staging/

# Local secrets / vars — never commit real values
*.auto.tfvars
secrets.tfvars
*.env
.env

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
